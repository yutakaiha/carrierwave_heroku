<%= form_with model: @post, local: true do |form| %>
  <p><%= form.text_field :title %></p>
  <!-- ***** 次を追加 ***** -->
  <p><%= form.file_field :image, accept: "image/png,image/jpg,image/jpeg,image/gif" %></p>
  <p><%= form.submit "送信" %></p>
<% end %>
